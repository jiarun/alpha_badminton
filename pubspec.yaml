name: my_flutter_app
description: A Flutter application for Web, Android, and iOS.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  provider: ^6.0.2
  flutter_secure_storage: ^4.2.1
  image_picker: ^0.8.4+4
  path_provider: ^2.0.8
  path: ^1.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/
